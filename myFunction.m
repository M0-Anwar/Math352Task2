function result = myFunction(x, y)
  % This function is used for testing the code

  # multiple values for the function 
  % result = 3 * (1 + x) - y;
  % result = exp(x)
  result = 1 + y*y + x*x*x;
endfunction